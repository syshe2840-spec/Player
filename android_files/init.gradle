allprojects {
    buildscript {
        repositories {
            maven { url = uri("https://alphacephei.com/maven/") }
        }
    }
    repositories {
        maven { url = uri("https://alphacephei.com/maven/") }
    }
}
